# 更新日志 · Changelog

本项目所有重要变更都会记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

This project's notable changes are documented here, following [Keep a Changelog](https://keepachangelog.com/) and [Semantic Versioning](https://semver.org/).

## [Unreleased]

## [0.3.1] — 2026-09-19

多系统支持整条线：三平台从「能打包」升级为「能运行」——CI 全链机械验证 + 启动冒烟，随批修复一批跨平台缺陷。

### 新增 · Added

- **磁盘命名安全单源**：Windows 保留名 / 非法字符 / 结尾点空格 / 控制字符 / 长度帽统一防护——项目与文件树手建名拒绝式校验（明确中文报错），章节文件名、导出、图片、材料派生名自动净化
- **CI 启动冒烟**：三平台各拉起应用主进程到就绪即退（Linux 经虚拟显示），原生模块 ABI 与启动路径回归每次推送即证
- **分发扩面**：macOS 补 zip（arm64 + x64）、Windows 与 Linux 补 arm64 安装包；Linux 发现 agy 命令时自动枚举常见安装路径（图形会话 PATH 受限场景），Windows 下 npm shim 形态给出专用引导

### 变更 · Changed

- Linux 文件监听原生化：递归监听自 Node 19.1 起三平台同一路径，外部改动自动刷新不再降级为手动刷新
- 主目录解析单源统一；agent 文件读取统一编码与行尾归一——GBK 中文文件与 CRLF 文件不再乱码或把回车符带进生成链路

### 修复 · Fixed

- 「移动文件」在 macOS / Linux 上目标已存在会被静默覆盖（丢数据）——现在显式拒绝
- macOS（默认文件系统大小写不敏感）下改名与提及台账的目录判定同类误判
- 休眠的 MCP 客户端补齐子进程错误处理（启用后不再可能崩主进程）
- agy 会话临时目录清扫加固，长活跃会话不再可能被误删

### 已知限制

- macOS 包未签名：首启需右键打开或 `xattr -cr` 绕过 Gatekeeper；应用内自动更新在签名落地前不可用
- arm64 产物（Windows / macOS / Linux）为机械验证级（CI 全链 + 启动冒烟实证，未真机实测）

## [0.3.0] — 2026-09-18

写作页全面改版，模型接入扩展到 Antigravity CLI，补齐用量观测与自动回退。

### 新增 · Added

- **写作页全面改版**：写章全程时间线（思考 / 调查 / 工具 / 正文流逐段可见），规划与终稿审批就地处理，终稿可直接手改、选中文字让 AI 精修
- **Antigravity CLI 接入**：经 Google 官方 agy CLI 直连调用 Gemini 系列模型（不经第三方反代中转）；配合工具桥，对话模型在一次性授权后可调用 Closure 的写作工具（写章、查设定、资料检索）
- **模型自动回退链**：任务档位可配回退模型，主力失败（配额 / 认证 / 超时 / 网关）自动切换并在对话中提示，消息带「回退自」标记
- **用量面板**：设置内「用量」页——本机调用计量，按模型 / 按任务档位分解，估算费用、失败与首字延迟，记录按保留窗自动清理
- **HTTP provider 参数面**：自定义请求头（网关鉴权 / 自建路由）、模型默认参数与额外请求体字段

### 变更 · Changed

- 写章链路重排：规划先行审核、多环收敛、异构审核槽位，产出快照以事件全程可观测
- 对话链路接入前缀缓存，长对话重复上下文不再全价重算

### 修复 · Fixed

- 拆书引擎韧性批（43 万字真书实测后集中修复）：长输出截断自动升帽重试、失败任务可重跑、派生 .md 章标记保全、重新摄取不再显示 0 章假态

## [0.2.0] — 2026-09-06

拆书引擎整条线上线：把读过的作品导入、拆解成可复用的写作知识。

### 新增 · Added

- **材料库**：批量导入 txt / docx / pdf / epub / srt / ass / vtt 等常见格式，自动识别章节结构、建立全文索引，带质量标记与出处信息
- **拆书引擎**：按档位与维度跑拆解——事实设定 / 读法 / 章评细批 / 风格特征逐层产出，关键节点人工把关；风格分析导出风格卡
- **经验蒸馏**：写作经验切条提炼，人审后沉淀为手艺页手艺卡
- **对话框附件**：文档自动解析为参考资料，图片交视觉模型识别（进度实时可见）

## [0.1.1] — 2026-08-30

### 新增 · Added

- **世界状态面板**：以世界事件为主线的读面，总览 / 时点 / 主体三级缩放，写章推进实时刷新
- **设定页**：八类资产卡可视化浏览与全字段编辑，设定文档聚合入口

### 修复 · Fixed

- 写章链首章冷启动死锁与假中断修复（章目录派生、done 事件在途守卫）
- preload sandbox 崩溃修复（此前导致全 app IPC 静默失效）

## [0.1.0] — 2026-08-28

Closure 首个公开发布。以 [OrisonSpace](https://github.com/LumenStorm/OrisonSpace)（Apache-2.0）为代码基底，叠加叙事操作系统层。

### 新增 · Added — 叙事层

- **多线叙事结构**：场景图统一管理故事骨架，多线并行推进、互相咬合
- **设定层**：资产卡、题材契约（GenreContract）、创作决策记录（StoryDecision）
- **结构工作台**：因果骨架 + 编排工作台两区联动结构页，同构锁步跨区高亮，章节轴按卷分带、小地图长篇导航
- **生成质量链**：写手两阶段成稿（自查缺口 → 调查清单 → 资料核实 → 动笔），上下文降级梯
- **情绪闭环**：每场戏先定目标情绪，写完自动检查落地，未落地打回修
- **信息差操控**：角色所知 / 读者所知分开记账，审核发现「说了不该知道的」「该揭的忘揭」
- **世界事件系统**：每章自动提取五类变化（物理/认知/情绪/关系/势力），事实真相与读者所知双层世界状态
- **修订保义**：AI 改动词级对比展示，改了什么、有没有偏原义一眼可验
- **百万字长程供给**：章摘要 checkpoint、完整性校验、提及台账
- **涟漪传播**：改设定沿叙事依赖图正向追查下游受影响章节
- **去 AI 味引擎**：内嵌 [llmlint](https://github.com/notnotype/llmlint) 静态引擎（数百条规则本地扫描）+ Lint 面板与修订闭环
- **双层审核漏斗**：L1 纯代码规则免费放行干净稿，L2 模型六组维度细审（每条意见必须附原文引证）
- **任务型模型路由与思考控制**：规划/正文/审核/研究等环节分别指定模型档位与推理力度
- **风格卡**：贴一段心仪小说，子代理分析成风格卡供写手/精修/规划参考
- **联网研究**：Tavily / Bocha / AnySearch 多供应商可配，资料员检索、导演核实、矛盾打回

### 变更 · Changed

- Agent Skill 改为 OpenCode 风格按需加载：`skill` tool 只加载 `SKILL.md` 内容和资源清单，不再将任意 `SKILL.md` 编译执行为 workflow DAG；后端强制执行 session mode 与 skill `allowed-tools` 权限
- 打包资源接线修复：craft KB 种子与 agent 契约 prompts 改走 `extraResources`（asar 外），打包版不再读空

### 继承 · Inherited

以下能力继承自 OrisonSpace 基座，详见上游：本地优先项目文件模型、isomorphic-git 版本时间线、IDE 式编辑器（分屏/Minimap/多标签/命令面板）、图片生成与编辑、任意 OpenAI 兼容端点接入（密钥本地加密）。

### 已知问题 · Known Issues

项目处于**早期开发阶段（Alpha）**：界面与功能可能变化，可能遇到各种 BUG。欢迎[提 Issue](https://github.com/chillison/Closure/issues)（附复现步骤与应用内「复制诊断」输出）。macOS / Linux 产物继承自上游架构，尚未充分实测。
